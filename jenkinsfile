pipeline {
    agent any
    environment{
        REPO_URL = "https://githib.com/prtkdave/devops"
               }
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello World"'
                sh '''
                    echo "Multiline shell steps"
                    ls
                    echo $REPO_URL
                   '''
                
            }
        }
    }
}

